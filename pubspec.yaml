name: qaffee_clean
description: Qaffee Restaurant App.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10
  form_field_validator: ^1.1.0
  http: ^1.2.0
  provider: ^6.1.1
  flutter_dotenv: ^6.0.0
  google_fonts: ^6.2.1   # moved here ✅

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/Illustrations/
    - assets/icons/
    - .env   # keep this if you want flutter_dotenv to load it
